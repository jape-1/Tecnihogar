# Variable de entorno para el build de PRODUCCION del frontend (Vercel).
# Debe apuntar al backend en EC2 (via el dominio HTTPS de Caddy) + el prefijo /api.
# En Vercel se configura en: Project Settings -> Environment Variables.
VITE_API_URL=https://tecnihogar.duckdns.org/api
